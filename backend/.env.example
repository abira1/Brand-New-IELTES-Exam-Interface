# Firebase Configuration
FIREBASE_DATABASE_URL=https://exam-interface-shah-sultan-default-rtdb.asia-southeast1.firebasedatabase.app

# Optional: Firebase Service Account Key (for production)
# Can be either:
# 1. Path to service account JSON file: /path/to/serviceAccountKey.json
# 2. JSON string: {"type":"service_account","project_id":"..."}
# If not provided, uses default credentials (Cloud Run, local emulator, etc.)
FIREBASE_SERVICE_ACCOUNT_KEY=

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5000,https://exam-interface-shah-sultan.web.app

# Server Configuration
PORT=8000
HOST=0.0.0.0

